# unex

Soft-reopen app for two people.

**Live:** https://emr-official.github.io/unex/

Source lives in `app/` (Next.js App Router + TS + Tailwind + Zustand).

Deploy: static export on `gh-pages` with `BASE_PATH=/unex`.
